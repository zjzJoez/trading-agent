"""Shadow Proposal Log — captures every trader-synthesizer proposal
even when gate/council blocks it, so we have a high-frequency record
for forward-evaluation of the LLM agent group's directional skill.

See migrations/008_shadow_proposals.sql for the table schema.
"""
